apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: dev
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /health
spec:
  type: ClusterIP
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 5000
